This project uses the Next.js App Router in `src/app` for route definitions.

The `src/pages` folder is kept as an architecture marker because the requested
project layout included it. Add legacy Pages Router routes here only if the app
needs them later.
